services:
  - type: web
    name: smart-reader-assistant
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: "10000"
    healthCheckPath: /api/health

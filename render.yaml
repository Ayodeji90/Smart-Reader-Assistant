services:
  - type: web
    name: smart-reader-assistant
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: "10000"
      - key: OCR_API_KEY
        value: "K89497956088957"
    healthCheckPath: /api/health
